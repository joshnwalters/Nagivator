<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nagivator">

    <uses-permission android:name="android.permission.VIBRATE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NaggyActivity"></activity>
        <activity
            android:name=".activity_sailing_activity"
            android:label="@string/title_activity_sailing_activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".afterSession"
            android:label="@string/title_activity_after_session"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".sailing_activity"
            android:label="@string/title_activity_sailing_activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".voyage"
            android:label="@string/title_activity_voyage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>